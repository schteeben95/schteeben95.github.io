{
  "name": "schteeben95.github.io",
  "version": "1.0.0",
  "private": true,
  "homepage": "https://stevenhan.net",
  "description": "Personal site for Steven X. Han - stevenhan.net",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist -t true"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "typescript": "^5.6.3",
    "vite": "^6.3.5"
  }
}
